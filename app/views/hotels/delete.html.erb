<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="hotel delete">
  <h2>Delete Hotel</h2>

  <%= form_for(:hotel, :url => {:action => 'destroy', :id => @hotel.id}) do |f| %>

    <p>Are you sure you want to permanently delete this hotel?</p>

    <p class="reference-name"><%= @hotel.hotel_name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Hotel") %>
    </div>

  <% end %>
</div>
