<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="festivals delete">
  <h2>Delete Festival</h2>

  <%= form_for(:festival, :url => {:action => 'destroy', :id => @festival.id}) do |f| %>

    <p>Are you sure you want to permanently delete this festival?</p>

    <p class="reference-name"><%= @festival.festival_name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Festival") %>
    </div>

  <% end %>
</div>
